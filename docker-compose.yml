services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      STORE_DB_PATH: ${STORE_DB_PATH:-data/store.db}
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
